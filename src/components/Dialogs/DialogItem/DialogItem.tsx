import React from "react";
import {DialogType, MessageType} from "../../redux/state";


type DialogItemType = {
    dialogs: DialogType[]
    messages: MessageType[]
}

/*
const DialogItem = (props:DialogItemType) => {
  let path = '/dialog/' + props.dialogs.
}*/
